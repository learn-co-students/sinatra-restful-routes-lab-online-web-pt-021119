<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>All recipes</title>
  </head>
  <body>
    <p><a href="/recipes/new"><button name="button">New recipe</button></a></p>

    <h1>All Recipes</h1>
    <% @recipes.each do |recipe| %>
    <hr>
    <strong>
      <a href="/recipes/<%= recipe.id %>">Recipe #<%= recipe.id %></a>
    </strong>
    <h2><%= recipe.name %></h2>
    <em>Cooking time: <%= recipe.cook_time %></em>
    <h5>Ingredients</h5>
    <p><%= recipe.ingredients %></p>
    <% end %>
  </body>
</html>
